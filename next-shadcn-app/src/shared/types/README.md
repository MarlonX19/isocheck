# Shared Types

Use esta pasta apenas para tipos transversais, como `PaginatedResponse`, `ApiError`, `UserSession` ou tipos usados por multiplas features.

Tipos de negocio especificos devem ficar em `src/features/<feature>/domain`.
